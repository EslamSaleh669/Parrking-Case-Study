package com.example.carstask.room

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider

//class CarsViewModelFactory(private val carRepo: CarRepo) : ViewModelProvider.Factory {
////        override fun <T : ViewModel> create(modelClass: Class<T>): T {
////            if (modelClass.isAssignableFrom(carsViewModel::class.java)) {
////                @Suppress("UNCHECKED_CAST")
////                return carsViewModel(carRepo) as T
////            }
////            throw IllegalArgumentException("Unknown ViewModel class")
////        }
//
//        }